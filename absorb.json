[
  {
    "node": "15019980fcea",
    "reason": "spec-review over 2026-08-20-derivation-status: S5 in test_conformance_and_content.md corrected from the integer-id era's 'type mismatch' to the shipped identity-hash pattern rejection (^[a-f0-9]{12}$). The scenario's observable contract is unchanged — the input still fails — only the stated rejection reason moves. No Go test asserts the old wording (validator/schema_checker_test.go carries no S5 counterpart, and no assertion mentions a type mismatch), so the correction owes no code."
  },
  {
    "node": "49a61e0d5737",
    "reason": "spec-review over 2026-08-20-derivation-status: S4's rationale in test_merkle_commands.md drops the stale 'filename-pattern' phrasing that contradicted arch_impact_classifier.md's no-path-parsing contract. The asserted behaviour (arch change classified arch_impl) is unchanged and already shipped; a describes==1 section owes no bead of its own regardless. No behaviour owed."
  }
]
