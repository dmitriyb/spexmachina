[
  {
    "node": "205e67ca4aad",
    "reason": "drift-fix spexmachina-xnec: writes down the eid-pairing rule and the cleanup-task-id rule the store already implements. Both shipped in 5854f7f (mapping/mapping_store.go builds the eid index before the walk; the removed arm keeps a paired cleanup receipt's task id). Correction of an under-specified leaf, no behaviour owed."
  },
  {
    "node": "21eae6071d42",
    "reason": "drift-fix spexmachina-xnec: names the lookup key in the removed-node scenario and enumerates the two scenarios that were already implemented but unnamed. mapping/mapping_store_test.go TestREQ_934d627f0e90_RemovedNodeReachableByCleanupTaskID already runs both file orders as subtests and asserts both keys resolve. The leaf catches up to the suite; no test is owed."
  },
  {
    "node": "4c1146bb7287",
    "reason": "drift-fix spexmachina-f6eh.13: puts the Retarget and Cleanup shape tables into the canonical field order the same leaf already fixes, and states that no shape table carries an order of its own. plan/types.go's Op struct already serializes deps before target; the leaf was the only place claiming otherwise. No behaviour owed."
  },
  {
    "node": "878c885a2517",
    "reason": "drift-fix spexmachina-f6eh.13: corrects the retarget op literal in the changeset.json example to deps-before-target, matching the canonical rule and the emitted bytes, plus a clause mirroring PlanCommand's tombstone rule into the journal-reads list. Documentation catching up to shipped behaviour; no behaviour owed."
  },
  {
    "node": "8aa1ab5ac102",
    "reason": "drift-fix spexmachina-f6eh.3: states the node-name resolution rule (graph lookup by kind, identity-hash fallback) and that node type is carried on the matched path, both already implemented in plan/action_classifier.go. The scenarios pinning them land on test_classification, which is minted rather than absorbed."
  }
]
