# Build output
bin/

# bv (beads viewer) local config and caches
.bv/

# br (beads) local files
.beads/.br_history
.beads/.br_recovery

# /converge pipeline run artifacts (diff.json, changeset.json, receipts.json, log)
.spex/

# Hook violation log (per-machine; format documented in docs/enforcement-rfc.md §5)
.claude/hook-violations.log

# Per-session skill-ownership markers (R14; format documented in docs/enforcement-rfc.md §9.1)
.claude/skill-sessions/
