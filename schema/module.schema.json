{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://spexmachina.dev/schema/module.json",
  "title": "Spex Machina Module",
  "description": "Schema for a module-level spec file (module.json). Defines module metadata, requirements, architecture components, implementation sections, and data flows.",
  "type": "object",
  "required": ["name"],
  "additionalProperties": false,
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "description": "Module name."
    },
    "description": {
      "type": "string",
      "description": "Module description."
    },
    "requirements": {
      "type": "array",
      "description": "Module-level requirements (functional and non-functional).",
      "items": { "$ref": "#/$defs/requirement" }
    },
    "components": {
      "type": "array",
      "description": "Architecture components with content links and cross-references.",
      "items": { "$ref": "#/$defs/component" }
    },
    "impl_sections": {
      "type": "array",
      "description": "Implementation sections describing how components are built.",
      "items": { "$ref": "#/$defs/impl_section" }
    },
    "data_flows": {
      "type": "array",
      "description": "Data flow definitions between components.",
      "items": { "$ref": "#/$defs/data_flow" }
    },
    "test_sections": {
      "type": "array",
      "description": "Test sections describing module integration and acceptance tests for components.",
      "items": { "$ref": "#/$defs/test_section" }
    },
    "apis": {
      "type": "array",
      "description": "External surface entry points this module provides (CLI subcommands, HTTP routes, library entry points).",
      "items": { "$ref": "#/$defs/api" }
    }
  },
  "$defs": {
    "requirement": {
      "$comment": "Module-level requirement. Extends the project-level requirement with preq_id to trace derivation from project requirements. IDs are 12-char hex identity hashes computed by schema.IdentityHash.",
      "type": "object",
      "required": ["id", "type", "title", "preq_id"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash ID (12-char hex), unique within the requirements array."
        },
        "preq_id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash of the project requirement this module requirement derives from."
        },
        "type": {
          "type": "string",
          "enum": ["functional", "non_functional"],
          "description": "Requirement type."
        },
        "title": {
          "type": "string",
          "minLength": 1,
          "description": "Short requirement title."
        },
        "description": {
          "type": "string",
          "description": "Detailed requirement description."
        },
        "depends_on": {
          "type": "array",
          "description": "Identity hashes of other requirements this one depends on (depends_on edge).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        }
      }
    },
    "component": {
      "type": "object",
      "required": ["id", "name", "content"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash ID (12-char hex), unique within the components array."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Component name."
        },
        "description": {
          "type": "string",
          "description": "Component description."
        },
        "content": {
          "type": "string",
          "minLength": 1,
          "description": "Relative path to the markdown file with detailed content (described_in edge to content leaf)."
        },
        "implements": {
          "type": "array",
          "description": "Requirement identity hashes this component implements (implements edge).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        },
        "uses": {
          "type": "array",
          "description": "Identity hashes of other components this one depends on (uses edge).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        }
      }
    },
    "impl_section": {
      "type": "object",
      "required": ["id", "name", "content"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash ID (12-char hex), unique within the impl_sections array."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Implementation section name."
        },
        "content": {
          "type": "string",
          "minLength": 1,
          "description": "Relative path to the markdown file with implementation details (described_in edge to content leaf)."
        },
        "describes": {
          "type": "array",
          "description": "Component identity hashes described by this section (describes edge).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        }
      }
    },
    "test_section": {
      "type": "object",
      "required": ["id", "name", "content"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash ID (12-char hex), unique within the test_sections array."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Test section name."
        },
        "content": {
          "type": "string",
          "minLength": 1,
          "description": "Relative path to the markdown file with test details (described_in edge to content leaf)."
        },
        "describes": {
          "type": "array",
          "description": "Component identity hashes described by this test section (describes edge).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        }
      }
    },
    "data_flow": {
      "type": "object",
      "required": ["id", "name", "content"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash ID (12-char hex), unique within the data_flows array."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "Data flow name."
        },
        "description": {
          "type": "string",
          "description": "Data flow description."
        },
        "content": {
          "type": "string",
          "minLength": 1,
          "description": "Relative path to the markdown file with data flow details (described_in edge to content leaf)."
        },
        "uses": {
          "type": "array",
          "description": "Component identity hashes involved in this data flow (uses edge).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        }
      }
    },
    "api": {
      "$comment": "An external surface entry point. No content file — an api hashes from these fields alone. Identity is <module>/api/<name>, computed by schema.IdentityHash.",
      "type": "object",
      "required": ["id", "name"],
      "additionalProperties": false,
      "properties": {
        "id": {
          "type": "string",
          "pattern": "^[a-f0-9]{12}$",
          "description": "Identity hash ID (12-char hex), unique within the apis array."
        },
        "name": {
          "type": "string",
          "minLength": 1,
          "description": "The exact external surface string as callers write it (e.g. \"spex diff\", \"GET /v1/specs/{id}\"). Never a signature."
        },
        "description": {
          "type": "string",
          "description": "What the entry point does."
        },
        "provided_by": {
          "type": "array",
          "description": "Identity hashes of components in this module that provide this entry point (module-local).",
          "items": {
            "type": "string",
            "pattern": "^[a-f0-9]{12}$"
          },
          "uniqueItems": true
        },
        "group": {
          "type": "string",
          "description": "Freeform grouping label for renderers (e.g. \"cli\", \"http\"). Spex never branches on it."
        }
      }
    }
  }
}
